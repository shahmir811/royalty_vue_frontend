export const loading = state => state.loading;

export const pageLoad = state => state.pageLoad;

export const customers = state => state.customers;

export const errors = state => state.errors;

export const fetchingCreditDetails = state => state.fetchingCreditDetails;

export const creditRecords = state => state.creditRecords;

export const selectedCustomerId = state => state.selectedCustomerId;

export const selectedCustomerName = state => state.selectedCustomerName;

export const fetchingPaymentDetails = state => state.fetchingPaymentDetails;

export const payments = state => state.payments;

export const serverRequest = state => state.serverRequest;

export const total = state => state.total;

export const dueAmount = state => state.dueAmount;

export const customerSales = state => state.customerSales;
