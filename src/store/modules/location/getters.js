export const loading = state => state.loading;

export const pageLoad = state => state.pageLoad;

export const locations = state => state.locations;

export const activeLocations = state => state.activeLocations;

export const errors = state => state.errors;

export const updateLocation = state => state.updateLocation;
