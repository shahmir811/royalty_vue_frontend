export const loading = state => state.loading;

export const pageLoad = state => state.pageLoad;

export const sales = state => state.sales;

export const errors = state => state.errors;

export const selectedSale = state => state.selectedSale;

export const statuses = state => state.statuses;

export const latestInvoiceNo = state => state.latestInvoiceNo;

export const fetchInvoiceNo = state => state.fetchInvoiceNo;

export const savingOnServer = state => state.savingOnServer;
